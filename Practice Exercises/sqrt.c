#include "math.h"


int max (int n1, int n2)
{
    if ( n2 > n1) return n2;
    return n1;
}

int maxadd 

double maxsqceil (double a) 
{
    double x = sqrt(a);
    double y = ceil(x);

    return y;
}